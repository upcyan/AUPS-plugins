window.AUPS_PLUGINS=window.AUPS_PLUGINS||{};
window.AUPS_PLUGINS.domainproxy=(function(){async function main(){const d=await api('GET','/api/domainproxy/status'),opts=(d.backends||[]).map(x=>'<option value="'+esc(x.name)+'" '+(x.name===d.backend?'selected':'')+'>'+esc(x.title)+'</option>').join('');view.innerHTML='<div class="card"><h2>域名代理</h2><div class="row"><input id="dpDomain" placeholder="proxy.example.com" value="'+esc(d.domain||'')+'"><input id="dpPrefix" placeholder="入口前缀，可留空" value="'+esc(d.prefix||'')+'"><select id="dpBackend">'+opts+'</select><button onclick="AUPS_PLUGINS.domainproxy.save()">保存并启动</button><button class="ghost" onclick="AUPS_PLUGINS.domainproxy.stop()">停止</button></div><div class="mut" style="margin-top:8px">内部服务仅监听 127.0.0.1，由所选反代插件负责 HTTPS、域名和公网入口；切换时自动迁移站点。</div><pre>'+esc(d.url||'')+'</pre></div>'}async function save(){await api('POST','/api/domainproxy/config',{domain:document.getElementById('dpDomain').value.trim(),prefix:document.getElementById('dpPrefix').value.trim(),backend:document.getElementById('dpBackend').value});await main()}async function stop(){await api('POST','/api/domainproxy/stop');await main()}return {title:'域名代理',sections:[{id:'main',title:'代理配置'}],go:main,open:main,save,stop};})();
